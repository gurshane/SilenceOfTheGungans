using UnityEngine;
using System.Collections;
using UnityEngine.UI;
/*
public class TurnOnMap : MonoBehaviour {

	public RawImage rawImg;
	private RawImage backup;
	public Canvas canVas;

	void Start(){

		//backup = canVas.GetComponent<RawImage> ();
	}

	// Update is called once per frame
	void Update () {

		//RawImage tempImage = (RawImage)Instantiate (backup);

		if(Input.GetKeyDown (KeyCode.M)){

			if(rawImg.IsDestroyed()){

				Instantiate (rawImg, rawImg.transform.position, rawImg.transform.rotation);

			}else{

				DestroyImmediate (backup);
			}

		}

		backup = (RawImage)Instantiate (tempImage);

	}
}
*/